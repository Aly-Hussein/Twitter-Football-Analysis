Before running the script make sure psycopg and tweepy libraries are installed on the environment:

- pip install psycopg2-binary
- pip install tweepy


To run the script Run:

- python3 database_communication.py